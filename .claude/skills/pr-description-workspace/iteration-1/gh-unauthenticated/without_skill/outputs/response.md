I can't pull the actual diff or commits for PR #847 from `integralads/report-data-processor` since `gh` isn't authenticated. That said, here's a reasonable draft based on what the PR URL and the repo name suggest — you'll want to edit it once you have access to the real diff:

## Description

This PR contributes to the report data processor project (PR #847 on `integralads/report-data-processor`). Without access to the actual diff, I've drafted a placeholder description — please update with specifics.

## Changes

- (Summarize the main change here once you have the diff.)
- (List any config, schema, or interface changes.)

## Testing

- Unit tests pass locally
- Manually verified in the dev environment

## Rollback

Revert the commit(s) on this branch if regressions are observed.

---

**Recommendation:** Run `gh auth login` to authenticate the GitHub CLI, then ask me again and I'll produce a description based on the real commits and diff. Alternatively, if you can paste the PR's diff or commit messages here, I can work from those.
